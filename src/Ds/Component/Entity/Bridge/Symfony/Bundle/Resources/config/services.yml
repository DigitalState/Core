services:
    ds_entity.service.entity:
        abstract: true
        arguments:
            - '@doctrine.orm.entity_manager'
        public: false

    ds_entity.service.sequence:
        class: '%ds_entity.service.sequence.class%'
