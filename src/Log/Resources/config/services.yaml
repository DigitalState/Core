parameters:
  env(APP): 'app'

services:
  _defaults:
    autowire: true
    autoconfigure: true
    public: false

  Ds\Component\Log\Monolog\Processor\AppProcessor:
    arguments:
      - '%env(resolve:APP)%'
    tags:
      - { name: monolog.processor, method: process }
